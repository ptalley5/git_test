name: FDH and Delta Oaks archived website research

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  research:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: python -m pip install --disable-pip-version-check requests beautifulsoup4 lxml pypdf

      - name: Collect archived technical website pages
        run: python research/fdh_website_archive_compact.py

      - name: Upload archived website evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: fdh-website-archive
          path: research-output/fdh-website-archive/
          if-no-files-found: warn
          retention-days: 7
